# Specifies the HTTP endpoint of your Prisma API
endpoint: http://localhost:4466

# Defines your models, each model is mapped to the database as a table
datamodel: datamodel.graphql

# Specifies the language and dir for the generated Prisma client
generate:
  - generator: typescript-client
    output: ../src/generated/prisma-client/

# Ensures Prisma client is re-generated after a datamodel change
hooks:
  post-deploy:
    - npx nexus-prisma-generate --client ./src/generated/prisma-client --output ./src/generated/nexus-prisma
# Seeds intial data into the database by running a script
# seed:
#   run: yarn ts-node ./prisma/seed.ts
